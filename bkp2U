#!/usr/bin/env bash

if [ ! -z "${DEV_BASE// }" ]
then
  cd $DEV_BASE/java/xlatticeCoreXML_java
  
  blListGen \
    -d ../xlatticeCoreXML_java \
    -1L  \
    -u $DVCZ_UDIR \
    $@
  
else
  echo "DEV_BASE is not defined"
fi
